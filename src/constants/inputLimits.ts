/** Maximum characters for name and email fields across auth and profile forms. */
export const AUTH_FIELD_MAX_LENGTH = 100;

/** Password length cap (must match server policy and every password `<input>`). */
export const PASSWORD_MAX_LENGTH = 15;
